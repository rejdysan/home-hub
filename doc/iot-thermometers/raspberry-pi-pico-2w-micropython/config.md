# Example for 'bedroom' thermometer
```python
wifi_ssid = 'wifi_name'
wifi_password = 'wifi_pass'
# If you’re using a local Mosquitto MQTT broker, you should pass the broker IP address without the port. For example: mqtt_server = b'192.168.1.79'
mqtt_server = b'192.168.0.X'
mqtt_username = b'pico_bedroom'
mqtt_password = b'password'
```